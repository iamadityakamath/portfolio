# Groq API key for the chatbot (api/chat.ts).
# Server-side only — never prefix this with VITE_, or it gets compiled into
# the client bundle and becomes public.
# Local dev: put the real value in .env (gitignored).
# Production: set it in Vercel → Settings → Environment Variables.
LLM_API_KEY=
